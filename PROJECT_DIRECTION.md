# AI Chatbot 前端实习项目方向说明

> 本文档只整理本项目已经形成的整体规划与方向判断，不收录 Zod、限流、Token 预算等额外技术问题的展开讨论。
>
> 当前状态（2026-08-11）：核心分支对话、流式交互、图片公网 URL 上传、可复现数据库迁移和自动化测试均已完成第一版闭环。项目进入求职展示和面试准备阶段，不再以堆叠新功能为目标。

## 1. 项目目标

本项目服务于百度、字节跳动、腾讯等互联网公司的前端实习求职。它不是为了建设一套完整的商业 AI 后端平台，也不是为了简单复刻 Vercel AI Chatbot，而是要成为一个能够体现以下能力的高质量前端项目：

- React 与 Next.js App Router 的实际理解；
- AI 流式交互中的复杂异步状态建模；
- 浏览器网络、文件和渲染能力的运用；
- 前端性能、可访问性、响应式体验与异常处理；
- TypeScript、测试、构建和代码边界等工程质量；
- 为保证项目真实可用而具备的必要全栈能力。

最终定位：**以“单向继承上下文的分支对话”为核心差异化能力，并围绕 AI 流式交互、复杂前端状态和现代 Next.js 架构进行深入设计的全栈前端项目。**

## 2. 方向判断原则

每项工作开始前都用下面四个问题判断优先级：

1. 它能否体现前端岗位需要的能力？
2. 它是否解决了当前项目中真实存在、可以复现的问题？
3. 面试时能否讲清问题、方案、权衡和验证结果？
4. 如果属于后端工作，它是否是项目安全、可靠、可部署所必需的？

满足前三项的前端工作优先；后端工作只做到第四项要求的必要程度。不能因为“生产级”听起来高级，就继续扩展与前端求职目标关系较弱的平台能力。

建议后续投入比例：

- 65%：前端交互、架构与性能；
- 20%：工程化、测试和质量；
- 15%：保证项目可信运行所需的后端底座。

## 3. 已完成的必要底座

### 3.1 工程与构建基线

项目已经完成依赖、脚本、环境变量、Lint、类型检查、单元测试、构建和 CI 等基础加固，并完成 Next.js 当前版本所需的配置适配。当前目标是保证开发与构建结果可重复验证，而不是继续搭建复杂的 DevOps 平台。

### 3.2 认证与安全边界

项目已经完成：

- 统一服务端认证入口；
- 使用运行时 Schema 统一校验接口输入；
- 在聊天、消息、删除和反馈的数据库查询中校验资源归属；
- 统一 Route Handler 和 Server Action 的错误模型；
- 限制模型 ID 为服务端允许的白名单。

这些工作用于消除明显的未登录访问、越权访问、非法参数和错误泄露风险。它们是可信项目的安全底线，但不是后续简历展示的主要卖点，完成后不继续向复杂权限平台扩张。

## 4. 当前功能边界与运行前提

下面这些内容不再是阻塞项目收尾的问题，而是明确的产品边界：

1. **模型选择是学习性实验**：它用于理解受控组件、请求参数和服务端模型映射；项目不要求接入第二个真实模型。
2. **图片上传链路已经完成第一版**：图片经认证后的 `/api/upload` 上传到 Vercel Blob，得到公网 HTTPS URL，并以结构化 `file part` 与文字组成同一条消息。运行者需要配置自己的 `BLOB_READ_WRITE_TOKEN`；当前 DeepSeek 文本模型不能理解图片，真正的视觉问答需要换成支持图片输入的 provider。孤儿文件回收和长期资源治理属于部署后的可选增强，不作为当前完成条件。
3. **数据库初始化已经可复现**：`pnpm db:migrate` 会从空 PostgreSQL 数据库按顺序执行版本化迁移，并使用 `app_migrations` 避免重复执行。通用迁移平台、数据库治理和复杂部署能力继续维持低优先级。

项目当前应把精力放在演示、代码理解和面试表达，而不是继续扩展后端平台能力。

## 5. 前端核心深化路线

### 5.1 AI 流式交互与异步状态

把聊天生成过程建模为清晰、可解释的状态流：空闲、提交、流式生成、完成、取消和失败。重点解决：

- 提交后的即时反馈；
- 停止生成与 `AbortController`；
- 失败后的重试或重新生成；
- 防止重复提交；
- 切换会话时取消旧请求；
- 网络异常和取消状态的正确 UI；
- 首次发送后 URL、聊天列表和当前消息状态的一致性；
- 自动滚动与用户主动向上阅读之间的冲突。

目标不是堆按钮，而是形成稳定的请求生命周期和可解释的状态模型。

### 5.2 React 与 Next.js 架构

围绕当前聊天链路梳理并解释：

- Server Component 和 Client Component 的职责边界；
- 服务端初始消息与客户端流式消息如何衔接；
- Route Handler 与 Server Action 的适用场景；
- 动态路由、URL 状态和聊天状态的一致性；
- 服务端刷新、缓存和客户端局部状态的关系；
- 组件、Hook、请求层和展示层的合理拆分。

只使用能解决实际问题的框架能力，不为了展示技术名词而过度抽象。

### 5.3 流式渲染与长会话性能

重点检查并优化：

- 流式片段到达时哪些组件重新渲染；
- 是否只有正在生成的最后一条消息更新；
- Markdown 与代码高亮是否被重复计算；
- 长历史是否采用分页或增量加载；
- 自动滚动是否造成频繁布局计算；
- 消息组件拆分和 memo 是否真正有效。

所有性能优化都要先建立可复现问题，再通过 React Profiler 或浏览器 Performance 工具记录优化前后结果，避免只写没有证据的 `useMemo`。

### 5.4 图片上传体验

后端存储做到安全、可靠、可部署即可，主要深度放在：

- 选择、拖拽和粘贴文件；
- 上传前预览；
- 类型与大小反馈；
- 上传中、失败、重试和删除状态；
- 多文件排列；
- 上传未完成时的发送行为；
- Object URL 的及时释放；
- 移动端体验和图片加载失败降级。

### 5.5 可访问性与响应式体验

覆盖键盘操作、焦点管理、语义化控件、屏幕阅读器可感知的生成状态、移动端布局和减少动态效果偏好。目标是让功能在不同输入方式和屏幕条件下仍然可用。

### 5.6 测试与质量证明

测试聚焦高价值链路，不追求形式上的高覆盖率：

- 分支是否只继承截至锚点的主对话，并保证主线不反向读取分支；
- 消息提交、流式生成与停止；
- 请求失败后的错误 UI；
- 删除会话与未登录访问；
- 图片上传成功与失败；
- 核心移动端或键盘操作。

## 6. 明确不作为当前重点的内容

在没有真实业务量和明确需求前，不建设：

- 完整 Redis/Upstash 分布式限流平台；
- 每日 Token 计费和多级用户配额；
- 模型成本分析后台；
- 复杂并发锁和任务队列；
- 完整调用监控平台；
- 多租户权限系统；
- RAG、Agent、知识库等与当前聊天体验无关的功能堆叠。

其中必要的请求大小限制、简单调用保护、服务端超时和模型输出上限可以作为安全边界实现，但不将它们扩展成后端平台项目。

## 7. 项目的差异化与面试表达

本项目来自 Vercel AI Chatbot 思路，最大的风险是被认为只是教程或开源项目复刻。因此每个核心改动都必须保留以下证据：

1. 原始实现存在什么可复现的问题；
2. 为什么选择当前方案；
3. 考虑过哪些替代方案和权衡；
4. 如何通过测试、性能数据或交互结果证明改善。

最终应形成三至四个能够深入讲解的前端亮点，而不是大量浅层功能。优先候选是：

- 单向继承上下文的分支对话及主线隔离；
- 流式响应状态机与异步竞态处理；
- 流式 Markdown 和长会话渲染性能优化；
- Server/Client Component 边界及核心链路测试。

其中分支对话是项目的第一核心亮点，其他能力围绕它提供稳定、可验证的使用体验。

## 8. 核心主线实现结果

项目核心主线“单向继承上下文的分支对话”已经完成。实际落地顺序为：

1. 统一消息身份，保证 UI 消息、AI SDK 流消息和数据库记录使用同一个 UUID；
2. 建立分支与主对话、锚点消息之间的最小数据关系；
3. 实现主对话前缀继承、服务端权威上下文组装和分支反向隔离；
4. 实现右侧分支面板、独立 `useChat`、移动端全屏布局和 URL 状态；
5. 补齐停止、重试、删除、刷新恢复、前进后退和键盘焦点管理；
6. 使用数据层、组件、接口和真实浏览器测试证明核心边界。

完整实现细节见 `BRANCH_CONVERSATION_IMPLEMENTATION.md`。

### 8.1 流式聊天交互闭环

当前已经把用户能够观察到的聊天生命周期整理为一致的交互规则：

- 所有发送入口经过同一个发送函数和提交守卫；
- 明确区分提交中、生成中、完成、用户取消和请求失败；
- 失败后能够恢复输入并重试，错误状态可以被清理；
- 停止生成后保留已生成内容，并向用户表达这是主动停止而不是系统错误；
- 首次发送、URL 更新和侧边栏刷新不互相干扰；
- 自动滚动只在用户仍靠近底部时跟随，用户主动向上阅读后停止抢夺滚动位置；
- 正在生成的消息不显示尚不可用的操作，避免反馈等动作与持久化时机冲突；
- 用组件测试验证重复提交、错误恢复和状态对应的按钮行为。

上述能力已经完成第一版：正常发送、流式生成、主动停止和请求失败拥有一致的界面行为；发送入口有提交守卫；错误后可以原地恢复或重新生成；用户主动向上滚动后会停止自动跟随，只有再次向下滚动且接近底部时才恢复跟随；消息持久化状态决定反馈和分支操作是否可用。

## 9. 历史流式交互实现审计（2026-07-22，已归档）

本节保留项目在 2026-07-22 的审计快照，用于解释后续改动为什么发生，不代表 2026-08-11 的当前代码状态。表中当时记录的统一发送入口、停止语义、错误恢复、滚动控制、稳定消息 ID 和组件测试等缺口现已完成；当前状态以第 8 节、阶段 A-D 实现状态和 README 为准。

### 9.1 已经完成

- 已使用 AI SDK `useChat` 管理消息、发送、状态、停止和错误；
- 已把服务端初始历史消息交给 `useChat`，并能够接收和显示流式回答；
- 已区分 `submitted` 与 `streaming` 的基础 UI：等待首个片段时显示“思考中”，两种忙碌状态都禁用输入并显示停止按钮；
- 已实现输入框 Enter 发送、Shift+Enter 换行和空消息拦截；
- 已有停止按钮并直接调用 AI SDK `stop()`；
- 已有基础错误展示；
- 已在正常 `onFinish` 后调用 `router.refresh()` 更新服务端组件中的聊天列表；
- 已在首次发送用户消息后使用 `history.replaceState` 更新聊天 URL；
- 已有基础的消息到底部自动滚动；
- 已加入 `submittingRef`，开始处理快速连点或连续 Enter 的重复提交问题；
- 历史页从数据库读取消息时会保留数据库消息 ID。

### 9.2 部分完成，但还存在缺口

| 能力       | 当前已有实现                                         | 仍存在的问题                                                           |
| -------- | ---------------------------------------------- | ---------------------------------------------------------------- |
| 状态驱动 UI  | 使用了 `submitted`、`streaming` 和 `error`          | 子组件把 `status` 声明成普通 `string`，状态规则散落在不同组件，没有统一派生行为                |
| 重复提交防护   | 输入框使用 `submittingRef`，忙碌时也会禁用输入                | 输入清空后锁会很快释放；提交函数本身没有根据 `status` 拒绝请求；建议词入口完全绕过该防护                |
| 发送入口     | 输入框和附件经过 `handleSubmit`                        | 建议词直接调用 `sendMessage`，形成第二条发送链路，两者校验和后续行为不一致                     |
| 停止生成     | 点击按钮可以调用 `stop()` 并中止客户端请求                     | 没有读取 `onFinish` 的 `isAbort`，因此用户停止、正常完成和网络断开没有明确语义区分             |
| 错误处理     | 页面能够显示 `error.message`                         | 没有 `clearError`、`regenerate`、关闭提示或无需刷新即可恢复的明确流程                  |
| 首次会话 URL | 第一条用户消息出现后会修改为 `/chat/[id]`                    | 逻辑依赖 `messages.length === 1` 的 effect，与请求成功、失败及持久化结果没有形成明确的一致性规则 |
| 自动滚动     | 每次 `messages` 或 `status` 变化都会滚动到底部             | 流式片段会反复触发平滑滚动；不判断用户是否主动向上阅读；外层区域和消息列表都设置了滚动，职责不唯一                |
| 消息反馈     | 每条 assistant 消息都会显示点赞按钮                        | 没有判断消息是否仍在生成、是否被停止、是否已经持久化；实时流消息 ID 与数据库新生成的消息 ID 也没有建立对应关系      |
| 服务端保存回答  | 正常生成完成后在 `streamText.onFinish` 写入 assistant 消息 | 没有处理中止路径，也没有消息完成状态；部分回答在客户端可见但刷新后可能消失                            |

### 9.3 尚未实现

- 没有统一的 `submitMessage` 作为输入框、建议词和未来重试操作的唯一发送入口；
- 没有基于 AI SDK `ChatStatus` 的类型安全 Props 和集中派生规则；
- 没有通过 `isAbort`、`isDisconnect`、`isError` 区分一次生成的结束原因；
- 没有“已停止生成”的界面语义；
- 没有对中断回答采取明确的一致性策略：保存并标记，或丢弃并从上下文移除；
- 没有错误清除和重新生成入口；
- 没有“用户位于底部附近才自动跟随”的滚动策略，也没有“回到底部”按钮；
- 没有在生成结束并完成持久化前隐藏反馈操作；
- 没有针对聊天提交、停止、错误恢复和滚动行为的组件测试；现有自动化测试只覆盖环境变量和登录页基础渲染。

### 9.4 审计结论

当前项目已经完成了流式聊天的基础演示，正常路径能够工作；下一阶段的价值不在于增加更多状态，而在于把已有状态、发送入口和结束原因统一起来，补齐停止、失败、滚动和数据一致性的异常路径。模型切换、复杂后端治理和部署能力不属于这一阶段。

## 10. 核心功能规划：分支对话

### 10.1 要解决的问题

主对话用于完成一个持续任务，但用户经常需要临时追问某个概念。直接在主对话中追问会让消息列表变乱，并让临时问题进入后续主线的模型上下文；另开普通对话又会丢失此前项目和任务语境。

分支对话用于同时满足：

- 继承主对话已有语境；
- 临时问题不显示在主消息列表；
- 分支内容不进入主线后续上下文；
- 用户可以在分支内围绕该问题连续追问多轮。

### 10.2 已确定的产品规则

1. 只允许从**已经完成并持久化的 assistant 整条回复**创建分支，不实现选中文字提问；
2. **一条 assistant 消息最多对应一个分支**；再次点击该消息的分支按钮时，直接打开已有分支；
3. 一个分支内部可以进行多轮问答；
4. 分支继承主对话从第一条消息到锚点 assistant 消息为止的全部上下文；
5. 分支不读取锚点之后新增的主对话消息；
6. 主对话永远不自动读取分支消息；
7. 第一版不支持分支中的分支；
8. 第一版不实现“引用回主对话”或合并分支，保留为未来扩展。

上下文关系示例：

```text
主对话：M1 → A1 → M2 → A2 → M3
                    └→ 分支：B1 → BA1 → B2 → BA2

A2 分支上下文：M1、A1、M2、A2、B1、BA1、B2
主对话后续上下文：M1、A1、M2、A2、M3
```

### 10.3 推荐数据模型

主对话和分支都复用 `chats` 表；分支本身是一个独立 chat，只增加来源关系：

```text
chats
├── id
├── user_id
├── title
├── parent_chat_id          nullable，指向主对话
├── branch_from_message_id  nullable，指向锚点 assistant 消息
└── created_at
```

约束规则：

- 主对话的 `parent_chat_id` 和 `branch_from_message_id` 都为空；
- 分支的两个字段必须同时存在；
- `branch_from_message_id` 增加唯一约束，数据库层保证一条消息最多一个分支；
- 创建分支时必须在同一条带所有权校验的查询中证明：锚点消息属于父对话，父对话属于当前用户，消息角色为 assistant；
- 分支自己的消息继续写入现有 `messages` 表，`messages.chat_id` 指向分支 chat；
- 主侧边栏只查询 `parent_chat_id IS NULL` 的主对话，不把分支混入普通历史列表；
- 删除分支不影响主对话；删除主对话时级联处理其分支。

第一版不复制主对话消息到分支表。分支保存锚点引用，在读取时组装上下文，避免重复数据和两个副本发生不一致。

### 10.4 必要前置：统一消息 ID

实现分支前，实时 assistant 消息的 UI ID 与数据库完成后生成的消息 ID 没有稳定对应关系，分支按钮可能引用数据库中不存在的 ID。阶段 A 已完成以下改造：

- 用户消息发送前生成规范 UUID，并以同一个 ID 写入数据库；
- assistant 开始流式生成前生成规范 UUID；
- AI SDK 返回给前端的 assistant 消息使用该 UUID；
- 流式完成后数据库 assistant 记录也使用同一个 UUID；
- 历史消息加载后继续保留该 ID；
- 只有完成持久化的 assistant 消息显示分支按钮。

最终保证：

```text
前端 UI messageId = 服务端流消息 ID = 数据库 messages.id
```

这一步同时修复实时消息点赞可能找不到数据库记录的问题。

### 10.5 上下文组装规则

主对话请求只使用主对话自己的消息。

分支请求由服务端或分支页面加载器组装：

```text
父对话从第一条消息到 branch_from_message_id
+ 分支 chat 自己的消息
+ 当前最新问题
```

必须通过锚点固定父对话截止位置。主对话以后新增的任何消息都不应自动进入已经存在的分支。

阶段三曾通过“继承前缀 + 分支消息”的 `initialMessages` 建立独立运行时，渲染时只显示分支自己的对话。阶段四已经进一步改为客户端只发送最新分支问题，服务端根据数据库中的父对话、锚点和分支历史权威组装完整上下文。

### 10.6 前端交互与组件边界

当前每条已经完成的 assistant 消息操作区提供“分支”按钮：

- 点击时先查询锚点是否已有分支，不立即创建数据库记录；
- 没有分支时打开临时草稿面板，用户第一次提交问题后才创建分支；
- 已有分支时，点击直接重新打开，不创建第二个；
- 正在流式生成、停止未落库或保存失败的消息不显示可用分支按钮。

桌面端使用右侧分支面板，移动端使用全屏 Sheet。面板至少包含：

- “基于这条回答”的锚点摘要；
- “已继承主对话截至此处的 N 条消息”提示；
- 分支自己的消息列表；
- 分支独立的输入框、停止状态、错误状态和自动滚动；
- 关闭分支入口。

主对话和分支使用两个独立的 `useChat` 实例：

```text
MainChat   → useChat({ id: mainChatId })
BranchChat → useChat({ id: branchChatId })
```

二者不能共享同一个可变 `messages` 数组。主页面只共享当前 `branchId`、锚点摘要和面板开关状态。

### 10.7 URL 与刷新恢复

使用查询参数保存当前打开的分支：

```text
/chat/main-chat-id?branch=branch-chat-id
```

打开分支时写入 `branch`，关闭时移除。这样可以支持：

- 刷新后恢复当前分支；
- 浏览器前进和后退；
- 直接定位某个主对话下的分支；
- 避免重要页面状态只存在于临时 `useState`。

服务端必须验证 URL 中的分支确实属于当前主对话和当前用户，不能只相信查询参数。

### 10.8 实施阶段

#### 阶段 A：消息身份一致性

- 统一用户和 assistant 的 UI/数据库 UUID；
- 确定消息完成、停止和持久化状态；
- 分支按钮只依赖稳定消息；
- 修复点赞对实时消息 ID 的依赖。

实现状态（2026-07-25）：已完成。项目现在使用统一 `ChatMessage`
契约区分浏览器请求状态和数据库消息状态；用户消息与 assistant 消息分别在发送前获得稳定 UUID，
并贯穿 UI、流式响应和数据库。正常回答保存为 `completed`，主动停止的部分回答保存为
`interrupted` 并在刷新后恢复；只有完整落库的 assistant 消息可以点赞或作为未来分支锚点。
点赞失败会回滚乐观状态并显示错误。阶段 A 未增加任何分支字段或分支 UI。

#### 阶段 B：数据关系与上下文

- 为 `chats` 增加父对话和锚点消息字段；
- 增加一条消息一个分支的唯一约束；
- 实现点击时只查询、首次提交时创建，以及读取和删除分支；
- 实现父对话前缀查询；
- 把所有权写进分支相关数据库查询；
- 主侧边栏排除分支。

实现状态（2026-07-29）：已完成。`chats` 已增加 `parent_chat_id` 和
`branch_from_message_id`，并通过外键、成对检查、唯一锚点约束和父对话索引保证最小数据关系。
点击入口使用只读查询，锚点没有分支时返回草稿状态；数据库分支只会在第三阶段的首次提交处理器调用
`createBranchOnFirstSubmit` 时创建，并与第一条用户消息在同一事务内落库，失败时不会留下空分支。
上下文读取分别返回截至锚点的主对话前缀和分支自己的消息，
主对话页面、删除入口与左侧历史列表均排除分支。阶段 B 未增加分支按钮、面板、独立 `useChat`
或 URL 恢复逻辑。

#### 阶段 C：分支 UI

- assistant 消息增加分支入口；
- 实现桌面侧栏和移动端 Sheet；
- 建立独立 `BranchChat`；
- 只展示分支自己的消息，继承前缀仅用于模型上下文；
- 同一锚点再次点击时恢复已有分支。

实现状态（2026-07-31）：已完成。只有状态为 `completed` 的 assistant 消息显示分支入口；
点击入口先执行只读查询，没有分支时打开临时草稿，第一次提交非空问题后才调用
`createBranchOnFirstSubmit`。桌面端使用右侧固定宽度面板，移动端使用全屏对话层，并支持
关闭按钮和 Escape 关闭。`BranchChat` 拥有独立 `useChat`、输入、停止、错误和
消息状态；其内部上下文由“主对话截至锚点的继承前缀 + 分支消息”组成，界面只截取并显示
分支自己的消息。首次提交复用浏览器生成的用户消息 ID，衔接阶段二已经落库的首条消息，
不会在 UI 或数据库中重复插入。分支消息列表不提供新的分支入口，因此第一版没有分支嵌套。
阶段 C 未加入 `?branch=`、刷新恢复或浏览器前进后退逻辑，这些仍属于阶段 D。

#### 阶段 D：路由、恢复与测试

- 使用 URL 查询参数保存活动分支；
- 刷新后恢复面板和分支历史；
- 覆盖上下文隔离、唯一分支、越权访问和异常恢复；
- 完成桌面端、移动端和键盘操作验证。

实现状态（2026-08-11）：已完成。持久化分支使用 `?branch=` 恢复刷新和浏览器历史；
非法或越权分支由服务端归属查询拒绝；分支请求只上传最新用户消息，模型上下文由服务端从数据库
重新组装。面板支持停止、未回答消息重试、二次确认删除、焦点陷阱、Escape 关闭、关闭后焦点恢复和
移动端全屏布局。Playwright 已验证登录页以及分支恢复、前进后退、Escape、移动端和删除闭环，
当前 2 个 E2E 用例全部通过。

### 10.9 核心验收场景

至少验证以下行为：

1. 从 A2 创建分支后，分支能够读取 A2 以及此前的主对话；
2. A2 之后新增的主消息不会进入该分支；
3. 分支内连续追问多轮时，会读取该分支此前的问答；
4. 主对话继续提问时，不会读取任何分支消息；
5. 同一条 assistant 消息重复点击只打开同一个分支；
6. 不同 assistant 消息可以各自拥有一个分支，但彼此不共享分支消息；
7. 刷新带有 `?branch=` 的页面后能够恢复正确分支；
8. 用户不能为其他用户的聊天或消息创建、读取分支；
9. 生成中、未落库或保存失败的 assistant 消息不能创建分支；
10. 删除分支后主对话内容保持不变。

### 10.10 第一版范围边界

第一版明确不做：

- 选中文字后创建分支；
- 一条消息创建多个分支；
- 分支中的分支；
- 自动把分支结论合并回主线；
- 手动“引用回主对话”；
- 分支搜索、知识图谱或树形编辑器；
- 多人共享分支；
- 同时后台生成多个分支。

未来可扩展“用户主动把某条分支结论引用回主对话”，但必须是显式操作，不能自动污染主线。

### 10.11 面试表达

核心表述：

> 我针对 AI 长对话中临时追问导致主线上下文污染的问题，设计了单向继承上下文的分支对话。每条 AI 回复最多关联一个可多轮追问的分支；分支继承主对话截至锚点的上下文，但主线不会读取分支内容。我通过独立聊天状态、稳定消息 ID、关系约束和 URL 状态实现了上下文隔离与刷新恢复。

面试时必须能够展示并解释：问题如何复现、为什么不是简单隐藏消息、主线和分支如何组装不同上下文、为什么统一消息 ID 是前置条件，以及如何用测试证明分支没有反向污染主线。
